# workflows/

How things get done — dev, test, build, deploy, ops. Format: the goal, the exact commands, and the
gotchas.

* [[development-and-testing]] — `npm test`, no build, UI preview mode.
* [[loading-and-reloading-the-extension]] — install unpacked; **reload after every code change**.
* [[publishing-to-the-chrome-web-store]] — listing copy, screenshot harness, `npm run package`.
* [[release-process]] — push to `release`; automated GitHub Release + Chrome Web Store submission.
