# features/

One page per non-trivial feature or flow. Format: what the user/caller experiences, the entry points
(`file:line`), and the mechanics. Cross-link to the `apps/` surface that hosts it.

* [[usage-collection]] — scheduled + on-demand sampling, two-tier strategy.
* [[timeline-chart]] — the custom canvas line chart.
* [[trend-forecast]] — linear-regression projection to limit/reset.
* [[toolbar-badge]] — the number on the extension icon.
* [[measurement-deletion]] — click a point to delete one measurement.
* [[data-export-and-retention]] — local storage, 90-day retention, CSV export, clear.
