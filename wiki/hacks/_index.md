# hacks/

One page per short-lived workaround expected to be removed. Format: what the hack is, why it's needed,
and the condition under which it can go. Move to **Removed** when gone.

Distinct from `decisions/` (deliberate, durable) and `tech-debt/` (accepted, longer-lived). The
background-tab fallback is **not** a hack — it's a deliberate design (see
[[2026-07-15-direct-fetch-then-background-tab-fallback]]).

## Active
_(none recorded — no short-lived workarounds identified at bootstrap.)_

## Removed
_(none)_
