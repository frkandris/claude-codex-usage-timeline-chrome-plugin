# apps/

One page per top-level runtime surface (entry point). Format: what it is, structure, key
functions with `file:line`, related pages. New surfaces are rare.

* [[background-service-worker]] — MV3 worker: collection, alarm, storage, badge.
* [[dashboard]] — canvas timeline UI + overview cards + interactions.
* [[settings]] — options page: interval, toggles, badge, export/clear.
